# Generated Sidecars

`scripts/native-host-build.mjs` writes target-suffixed `vibe-native-host`
binaries into this directory immediately before Tauri bundles the app.

The binaries are build artifacts and must not be committed.
